<h2><%= listing.name %></h2>
<% if listing.photo.attached? %>
  <img class="card-img-top" style="border-radius: 5px" src="<%= cl_image_path listing.photo.key %>">
  <% end %>
  <p class="mx-1"><%= listing.price %>€/day</p>
<%= link_to 'Book now', listing_path(listing), class: "btn btn-gradient" %>
