<%= render user_signed_in? ? "../views/navbar_loggedin" : "../views/navbar_notloggedin" %>


<div class="container">
  <div class="d-flex">
    <div>
      <h1 class="mt-4"><%= @listing.name %></h1>
      <div class="card-trip mt-2">
        <img src="<%= cl_image_path @listing.photo.key %>">
        <div class="card-trip-infos">
          <div class="d-flex flex-column justify-content-between h-100">
            <div>
              <h2><%= @listing.name %> | <%= @listing.vehicle_type %></h2>
              <p><%= @listing.description %></p>
            </div>
            <div>
              <h2>Where can you find me?</h2>
              <p><%= @listing.country %></p>
              <p><%= @listing.city %></p>
            </div>
          </div>
          <h2 class="card-trip-pricing"><%= @listing.price %>€</h2>
          <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
        </div>
      </div>
    </div>
    <div class="d-flex update-listing flex-column">
        <%= link_to 'Update', edit_listing_path(@listing), class: "btn btn-gradient mt-3" %>
        <%= link_to 'Delete', listing_path(@listing), class: "btn btn-gradient", method: :delete, data: { confirm: 'Are you sure you want to delete this listing?' } %>
      </div>
  </div>
</div>



<%= render "../views/footer" %>
